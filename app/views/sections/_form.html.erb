<%= form_with(model: section) do |form| %>
  <% if section.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(section.errors.count, "error") %> prohibited this section from being saved:</h2>

      <ul>
        <% section.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, "Section Name", style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :room, "Room", style: "display: block" %>
    <%= form.text_field :room %>
  </div>

  <div>
    <%= form.label :timeslot, "Timeslot", style: "display: block" %>
    <%= form.text_field :timeslot %>
  </div>

  <div>
    <%= form.label :subject_id, "Subject", style: "display: block" %>
    <%= form.collection_select :subject_id,
                                Subject.order(:name),
                                :id,
                                :name,
                                { prompt: "Select Subject" } %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>